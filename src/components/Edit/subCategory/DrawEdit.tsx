const DrawEdit = ({ selectKey }: { selectKey: string }) => {
  return (
    <div>
      <button>
        ペン
      </button>

      <button>
        消しゴム
      </button>
    </div>
  )
}

export default DrawEdit